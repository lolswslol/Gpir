import { NotAvailableDirective } from './not-available.directive';

describe('NotAvailableDirective', () => {
  it('should create an instance', () => {
    const directive = new NotAvailableDirective();
    expect(directive).toBeTruthy();
  });
});
