import { DeleteZeroDirective } from './delete-zero.directive';

describe('DeleteZeroDirective', () => {
  it('should create an instance', () => {
    const directive = new DeleteZeroDirective();
    expect(directive).toBeTruthy();
  });
});
