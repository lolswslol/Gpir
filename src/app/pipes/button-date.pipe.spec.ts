import { ButtonDatePipe } from './button-date.pipe';

describe('ButtonDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ButtonDatePipe();
    expect(pipe).toBeTruthy();
  });
});
