import { ProjectColorPipe } from './project-color.pipe';

describe('ProjectColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectColorPipe();
    expect(pipe).toBeTruthy();
  });
});
